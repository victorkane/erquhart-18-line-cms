backend:
  name: github
  repo: victorkane/erquhart-18-line-cms
  branch: main

media_folder: images/uploads

collections:
  - name: post
    label: Post
    folder: posts
    create: true
    fields:
      - { name: title, label: Title }
      - { name: body, label: Body, widget: markdown }
